<div class="container mt-5">
    <h2 class="mb-4 text-primary">Employee Form</h2>

    <form id="employeeForm">
        <div class="mb-3">
            <label for="Name" class="form-label">Name</label>
            <input type="text" id="Name" name="Name" class="form-control" required />
        </div>

        <div class="mb-3">
            <label for="Email" class="form-label">Email</label>
            <input type="email" id="Email" name="Email" class="form-control" required />
        </div>

        <div class="mb-3">
            <label for="Age" class="form-label">Age</label>
            <input type="number" id="Age" name="Age" class="form-control" required />
        </div>

        <button type="submit" class="btn btn-success">Submit</button>
    </form>

    <div id="result" class="mt-3"></div>
</div>

<!-- jQuery CDN (uncomment this line if jQuery is not included elsewhere) -->
<!-- <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script> -->

<script>
    // $(document).ready(function () {
    //     $('#employeeForm').submit(function (e) {
    //         e.preventDefault();

    //         const data1 = {
    //             Name: $('#Name').val(),
    //             Email: $('#Email').val(),
    //             Age: $('#Age').val()
    //         };

    //         $.ajax({
    //             type: 'POST',
    //             url: '@Url.Action("AddEmployee", "Home")',
    //             data: data1,
    //             success: function (response) {
    //                 $('#result').html('<div class="alert alert-success">Employee added successfully!</div>');
    //                 $('#employeeForm')[0].reset();
    //                 console.log(response);
    //             },
    //             error: function (xhr, status, error) {
    //                 $('#result').html('<div class="alert alert-danger">An error occurred: ' + xhr.responseText + '</div>');
    //                 console.error("Error:", error);
    //             }
    //         });
    //     });
    // });

    $(document).ready(function (){

        $('#employeeForm').submit(function (e){
            e.preventDefault();

            const userdata={
                 Name: $('#Name').val(),
                 Email:$('#Email').val(),
                 Age: $('#Age').val()
            }

            $.ajax({

                type:'POST',
                url:'@Url.Action("AddEmployee","Home")',
                data:userdata,
                success:function(reponse){
                        $("#employeeForm")[0].reset();
                                                     $('#result').html('<div class="alert alert-success">Employee added successfully!</div>');

                        setTimeout(function(){
                             $('#result').html('');
                        },2500)
       
                },
                error: function(xhr,status,error){
                        console.log(xhr.resposnse,error,status);
                }

            })
        })



    })
</script>
